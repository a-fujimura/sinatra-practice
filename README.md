# 環境導入手順
1. リポジトリをクローンします。
`git clone https://github.com/a-fujimura/sinatra-practice.git`
2. クローンしたフォルダに移動し、作業用ブランチに切り替えます。
```bash
cd sinatra-practice
git checkout f-work
``` 
3. Gemをインストールします
`bundle install`
